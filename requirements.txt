# Framework web
fastapi~=0.115.12
uvicorn

# Pydantic
pydantic~=2.11.4
email-validator

# Supabase
supabase~=2.10.0
python-dotenv
tzdata

# HTTP requests (pour renvoi d'email de confirmation)
requests

# Validation JWT Supabase sans appel réseau à GoTrue (évite erreurs SSL / reset)
PyJWT~=2.10.0

# Redis (cache, pub/sub, rate limit)
redis>=5.0.0,<6
